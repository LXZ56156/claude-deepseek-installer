# 发布前验收清单

> 开发者用。每次 release 打包前逐项执行，确认全部通过后再上传。

---

## 1. 打包

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\build-release.ps1 -Version "1.3.0"
```

预期: 5 步全部通过，生成 .zip + .sha256。

---

## 2. 解压到全新目录

不要在源码目录测试。解压到桌面或临时目录：

```powershell
Expand-Archive .\release\*.zip -DestinationPath .\release-check\
```

---

## 3. 检查不应包含的文件

```powershell
cd .\release-check\
```

以下命令必须无输出：

```powershell
# 不应包含内部文件
Get-ChildItem -Recurse -Include ".git", "logs", "backup", "reports", "release" -Directory
Get-ChildItem -Recurse "CLAUDE.md", ".gitignore", "leak-test.md", "report*.txt", "*.tmp", "*.log"
Get-ChildItem -Recurse -Filter "build-release.ps1"

# 不应包含的文件名模式
ls -Recurse | Where-Object { $_.Name -match 'CLAUDE\.md|build-release\.ps1|\.gitignore|leak-test|report.*\.txt' }
```

---

## 4. 检查 .ps1 文件 BOM

```powershell
$b = [System.IO.File]::ReadAllBytes(".\Start-Here.ps1")
$b[0..2] -join " "
```

结果必须是: `239 187 191`

对所有 .ps1 文件验证:

```powershell
Get-ChildItem -Recurse -Filter *.ps1 | ForEach-Object {
    $b = [System.IO.File]::ReadAllBytes($_.FullName)
    if ($b[0] -ne 239 -or $b[1] -ne 187 -or $b[2] -ne 191) {
        Write-Host "[FAIL BOM] $($_.Name)"
    } else {
        Write-Host "[OK] $($_.Name)"
    }
}
```

全部必须 [OK]。

---

## 5. 检查 .cmd 文件编码

```powershell
Get-ChildItem -Recurse -Filter *.cmd | ForEach-Object {
    $b = [System.IO.File]::ReadAllBytes($_.FullName)
    $hasBom = ($b[0] -eq 239 -and $b[1] -eq 187 -and $b[2] -eq 191)
    $nonAscii = ($b | Where-Object { $_ -gt 127 }).Count
    if ($hasBom -or $nonAscii -gt 0) {
        Write-Host "[FAIL] $($_.Name) BOM=$hasBom nonASCII=$nonAscii"
    } else {
        Write-Host "[OK] $($_.Name)"
    }
}
```

全部必须 [OK]。

---

## 6. PowerShell 5.1 AST parse

在 Windows PowerShell 5.1 中运行（不要在 PowerShell 7 中运行）：

```powershell
Get-ChildItem . -Recurse -Filter *.ps1 | ForEach-Object {
    $tokens = $null
    $errors = $null
    [System.Management.Automation.Language.Parser]::ParseFile($_.FullName, [ref]$tokens, [ref]$errors) | Out-Null
    if ($errors.Count -gt 0) {
        Write-Host "[FAIL] $($_.FullName)" -ForegroundColor Red
        $errors
    } else {
        Write-Host "[OK] $($_.FullName)" -ForegroundColor Green
    }
}
```

全部必须 [OK]。

---

## 7. 双击测试

在解压目录中依次双击以下文件，确认能正常启动，不乱码、不报 ParserError:

- `开始安装.cmd` — 应显示菜单
- `Start-Install.cmd` — 应显示菜单
- `一键诊断.cmd` — 应生成诊断报告
- `Run-Diagnostics.cmd` — 应生成诊断报告
- `恢复或卸载配置.cmd` — 应进入配置管理
- `Restore-Config.cmd` — 应进入配置管理

---

## 8. 故意泄露测试

在源码目录（非解压目录）中：

```powershell
Set-Content .\leak-test.md "DEEPSEEK_API_KEY=sk-leaktest0123456789abcdef0123456789"
powershell -ExecutionPolicy Bypass -File .\scripts\build-release.ps1 -SkipSha256
Remove-Item .\leak-test.md
```

预期: 打包失败，提示 `leak-test.md` 包含疑似真实 API Key，exit code 1。

---

## 9. 干净打包（最终）

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\build-release.ps1 -Version "1.3.0"
```

预期: 全部通过，记录最终 SHA256。

---

## 10. 最终 SHA256

```
SHA256: ________________________
日期:   ________________________
```

---

## 11. 快速冒烟（Windows PowerShell 5.1）

```powershell
# 检测安装（不安装），只验证菜单和环境检测
$env:CCDI_API_KEY = "sk-dummy"
.\Start-Here.ps1 -SkipDisclaimer -SkipApiTest -NonInteractive
Remove-Item Env:\CCDI_API_KEY
```

预期: 进入懒人安装流程，环境检测步骤正常输出中文。

---

> 注意: 本文件是开发者验收文档，不进入买家 release 包。
