# 用户体验验证清单

> 人工测试矩阵，覆盖安装/诊断/配置/卸载全流程。
> 标记说明: [OK] 通过 | [ERROR] 失败 | [WARN] 需关注 | — 不适用

---

## 一、最低系统要求验证

| 编号 | 场景 | 预期结果 | 结果 |
|------|------|---------|------|
| R1 | Win11 x64 16GB | 全部通过，正常安装 | |
| R2 | Win10 1809+ x64 4GB | 全部通过，正常安装 | |
| R3 | Win10 1809+ ARM64 | 架构通过，正常安装 | |
| R4 | Win10 Build < 17763 | ERROR: 提示升级 Windows | |
| R5 | 32 位系统 | ERROR: 提示需要 64 位 | |
| R6 | 内存 < 4GB | ERROR: 提示内存不足 | |

---

## 二、Windows 环境测试

### 2.1 操作系统

| 编号 | 场景 | 预期结果 | 结果 |
|------|------|---------|------|
| W1 | Win11 正常电脑 | 检测通过，安装成功 | |
| W2 | Win10 1809+ | 检测通过，安装成功 | |
| W3 | Win10 < 1809 | ERROR 停止，提示升级 | |
| W4 | PowerShell 5.1 | 正常 | |
| W5 | PowerShell 7 | 正常 | |
| W6 | 中文用户名 | 路径处理正确，不乱码 | |
| W7 | 路径有空格 | 安装正常，不报错 | |

### 2.2 Claude Code 安装状态

| 编号 | 场景 | 预期结果 | 结果 |
|------|------|---------|------|
| W8 | Claude 未安装 | Native Install 优先，失败则 npm fallback | |
| W9 | Claude 已安装 | 检测到，询问是否更新 | |
| W10 | Node.js 未安装 | winget 安装提示，或提示手动安装 | |
| W11 | Node.js < 18 | 提示升级 | |
| W12 | Node.js >= 18 | 通过检测 | |

### 2.3 配置文件状态

| 编号 | 场景 | 预期结果 | 结果 |
|------|------|---------|------|
| W13 | 无 settings.json | 新建并写入配置 | |
| W14 | 有 settings.json | 备份后合并，保留原有字段 | |
| W15 | settings.json 损坏 | 备份后重建 | |
| W16 | env 为 null | 安全处理，不崩溃 | |
| W17 | env 为 string/array | 安全处理，log WARN | |
| W18 | 已有 permissions/hooks/mcpServers | 合并后不丢失 | |
| W19 | 已有自定义 env 字段 | 合并后不丢失 | |

### 2.4 API 测试场景

| 编号 | 场景 | 预期结果 | 结果 |
|------|------|---------|------|
| W20 | DeepSeek 正常响应 200 | API 测试通过 | |
| W21 | DeepSeek 401 | 提示 Key 错误，继续流程 | |
| W22 | DeepSeek 402 | 提示余额不足，继续流程 | |
| W23 | DeepSeek 429 | 提示限流，继续流程 | |
| W24 | DeepSeek 5xx | 提示官方异常，继续流程 | |
| W25 | DNS 解析失败 | 提示网络问题 | |
| W26 | API 测试跳过 (-SkipApiTest) | 标注 SKIP | |

---

## 三、诊断报告验证

| 编号 | 场景 | 预期结果 | 结果 |
|------|------|---------|------|
| D1 | report.txt 生成 | 存在且为分享版 | |
| D2 | reports/report-YYYYMMDD-HHMMSS.txt | 存在，与 report.txt 内容相同 | |
| D3 | reports/full-report-YYYYMMDD-HHMMSS.txt | 存在，包含真实路径 | |
| D4 | report.txt 不含完整 API Key | sk- 被脱敏为 sk-xx****xxxx | |
| D5 | report.txt 不含 C:\Users\真实用户名 | 替换为 %USERPROFILE% | |
| D6 | full-report-xxx.txt 不含完整 API Key | API Key 脱敏但不替换路径 | |
| D7 | 最低要求检查分区存在 | 报告中包含"最低要求检查" | |
| D8 | 不满足最低要求时报告有建议 | 每项 ERROR 都有对应建议 | |

---

## 四、配置管理验证

| 编号 | 场景 | 预期结果 | 结果 |
|------|------|---------|------|
| C1 | 从备份恢复配置 | 选择备份文件，恢复成功 | |
| C2 | 仅移除 DeepSeek env | DeepSeek 字段移除，自定义字段保留 | |
| C3 | 删除整个配置文件 | 备份后删除 | |
| C4 | 备份失败禁止写入 | 阻止操作，提示原因 | |
| C5 | 无备份可恢复 | 提示无可用备份 | |

---

## 五、状态文件验证

| 编号 | 场景 | 预期结果 | 结果 |
|------|------|---------|------|
| S1 | Native Install 成功 | state.json: method=native, status=Installed | |
| S2 | npm fallback 成功 | state.json: method=npm, status=Installed | |
| S3 | winget Node.js 安装 | state.json: method=node-via-winget, status=NeedsRestart | |
| S4 | Claude 已存在 | state.json: wasAlreadyInstalled=true, method=existing | |
| S5 | API 测试通过 | state.json: lastApiTest=passed | |
| S6 | API 测试失败 | state.json: lastApiTest=failed | |
| S7 | API 测试跳过 | state.json: lastApiTest=skipped | |
| S8 | 卸载时读取 state | 显示安装方式信息 | |
| S9 | 无 state.json 卸载 | 提示"无法确认安装方式"，仅提供配置管理 | |

---

## 六、WSL 验证

| 编号 | 场景 | 预期结果 | 结果 |
|------|------|---------|------|
| L1 | WSL Ubuntu 20.04+ | 检测通过 | |
| L2 | WSL Ubuntu < 20.04 | ERROR: 提示升级 | |
| L3 | WSL 非 Ubuntu 发行版 | WARN: 未充分测试 | |
| L4 | 无 WSL | Windows 流程 SKIP WSL 项 | |
| L5 | 无 node/python3 | 正确提示安装 | |
| L6 | sudo 不可用 | 不影响检测（不强制执行 sudo） | |
| L7 | install_wsl.sh 独立运行 | 不依赖 Windows 脚本 | |
| L8 | 方式 B 路径含单引号 | 警告并拒绝自动调用 | |
| L9 | 方式 B wslpath 失败 | 提示改用方式 A | |
| L10 | 日志不含完整 API Key | 脱敏处理 | |
| L11 | Windows 和 WSL 配置不一致 | doctor 提示 | |

---

## 七、自动验证

运行以下命令验证代码级正确性：

```powershell
# Windows
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\ux-check.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build-release.ps1
```

```bash
# WSL/Linux
bash scripts/ux-check.sh
bash scripts/check.sh
bash -n install_wsl.sh
```
