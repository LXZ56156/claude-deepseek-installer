# v1.3.2-rc 验收与修复交接

> 日期: 2026-06-10  
> 分支: `release/v1.3.2-rc`  
> 当前最新已推送提交: `6e01a04`  
> 用途: 记录 Windows 原生自动化验收、阻塞问题、修复提交和后续交接事项。

## 1. 背景

本轮目标是对 `release/v1.3.2-rc` 做 Windows 原生自动化全量发布验收，并保证：

- Windows 本地路径验收，不使用 `\\wsl.localhost` / `\\wsl$`。
- 自动化验收不使用真实 DeepSeek API Key。
- 自动化验收不请求真实 DeepSeek API。
- 自动化验收不执行真实 Claude 安装、`winget`、`npm install/update`。
- `Start-Here -TestSafe`、`repair-deps -TestSafe`、`doctor -ShareSafe -SkipApiTest` 都必须安全运行。
- 真实 `%USERPROFILE%\.claude\settings.json` 不被创建或修改。
- release ZIP 可构建、可解压、内容干净，并且 ZIP 内脚本可运行 TestSafe/ShareSafe 验证。

## 2. 当前状态

- 当前分支应保持为 `release/v1.3.2-rc`。
- 当前最新提交应为 `6e01a04` 或后续提交。
- `6e01a04` 已 push 到 `origin/release/v1.3.2-rc`。
- 工作区运行产物可能存在于 `.sandbox/`、`logs/`、`backup/`、`reports/`、`release/`、`report.txt`，这些均不应加入 Git。

重要状态说明：

- 在 `4433b64` 时执行过一次 Windows 原生全量验收，结果通过，并生成过 release ZIP。
- 之后又追加了 `369a9cf`、`e708863`、`6e01a04`，分别修复 `claude doctor` 等待、API 异常处理、诊断工具 `claude doctor` 等待问题。
- 因此正式发布前应从 `6e01a04` 或更新提交重新跑完整 Windows 原生全量验收，并重新构建最终 release ZIP。

## 3. 已修复问题记录

### 3.1 压缩包临时路径未 BLOCK

- 等级: P1
- 失败现象:
  - `TEMP\Rar$EXa123\ClaudeDeepSeek` 返回 `WARN / IsBlocked=False`
  - `TEMP\7zO123456\ClaudeDeepSeek` 返回 `WARN / IsBlocked=False`
- 根因:
  - `Test-IsZipInternalPath` 使用 `Contains()` 做字面匹配。
  - marker 写成 `"\\rar$"`、`"\\7z"` 等双反斜杠字符串。
  - PowerShell 字符串中的反斜杠不是转义符，真实 Windows 路径只有单反斜杠，导致不命中。
- 修复提交:
  - `bb5d159` `fix(path): block archive temp extraction paths`
- 修复内容:
  - 路径规范化。
  - 路径段级正则匹配。
  - `Rar$...` 明确 BLOCK。
  - TEMP 下 `7z...` 明确 BLOCK。
  - TEMP 下 `.zip` / `Temp1_*.zip` 明确 BLOCK。
  - 普通 TEMP 仍为 WARN。
  - 非 TEMP 的 `D:\tools\7zip-helper\...` 不 BLOCK。

### 3.2 空 env 在 StrictMode 下恢复失败

- 等级: P1
- 失败现象:
  - `uninstall-config.ps1 -RestoreLatest -Yes -NonInteractive` 在沙盒恢复路径失败。
  - 错误: `The property 'Name' cannot be found on this object.`
- 触发链路:
  - `RemoveDeepSeekEnv` 后 `settings.json` 保留 `env = {}`。
  - `RestoreLatest` 前 `Show-StateAndCurrentConfig` 调用 `Get-DeepSeekConfigStatus`。
  - StrictMode 下访问空 PSCustomObject 的 `.PSObject.Properties.Name` 抛错。
- 修复提交:
  - `4433b64` `fix(config): handle empty env under strict mode`
- 修复内容:
  - 新增安全属性枚举 helper。
  - `Get-DeepSeekConfigStatus` 对空 env 返回未配置，不抛错。
  - `Get-ApiKeyFromConfig` 对空 env 返回 `$null`，不抛错。
  - 未关闭 StrictMode。
  - 未改变 remove/restore 核心语义。

### 3.3 安装流程 claude doctor 等待过久

- 等级: P2/P3 UX
- 用户反馈:
  - Native Install 成功后显示 `运行 claude doctor...`，很久之后才出现 `按回车键继续...`。
- 根因:
  - 安装流程同步等待 `claude doctor`，输出被捕获，用户看不到实时输出。
- 修复提交:
  - `369a9cf` `fix(ux): bound claude doctor quick check`
- 修复内容:
  - 安装流程中的 `Invoke-ClaudeDoctorSafe` 改为最多等待 30 秒。
  - 每 10 秒提示一次。
  - 超时或异常不阻断安装流程。

### 3.4 API smoke test 异常对象缺少 Response 属性

- 等级: P1
- 用户反馈:
  - Step 5/7 DeepSeek API 测试报错:
    - `在此对象上找不到属性“Response”。请确认该属性存在。`
- 根因:
  - `Test-DeepSeekApiAnthropic` 的 catch 直接访问 `$_.Exception.Response`。
  - 某些网络异常对象没有 `Response` 属性，StrictMode 下直接抛错。
- 修复提交:
  - `e708863` `fix(api): handle response-less smoke test errors`
- 修复内容:
  - 增加安全属性枚举。
  - 异常对象先判断是否存在 `Response` 属性。
  - 无 `Response` 的网络错误返回结构化错误，不再未预期崩溃。
  - API 响应解析也避免直接访问不存在的 `content` / `choices` 属性。
  - `scripts/check.ps1` 增加本地关闭端口回归，不请求 DeepSeek。

### 3.5 doctor.ps1 中 claude doctor 仍等待过久

- 等级: P2/P3 UX
- 用户反馈:
  - 环境诊断工具里的 `正在运行 claude doctor...` 仍然等很久。
- 根因:
  - `doctor.ps1` 中另有一处直接调用 `Invoke-CommandSafe -Command "claude" -Arguments @("doctor")`。
  - 该处没有使用安装流程中已修复的 30 秒上限逻辑。
- 修复提交:
  - `6e01a04` `fix(doctor): bound claude doctor diagnostics`
- 修复内容:
  - `doctor.ps1` 中 `claude doctor` 改为快速诊断，最多等待 30 秒。
  - 每 10 秒输出进度提示。
  - 超时或非零退出码记为 WARN，不阻断后续诊断。
  - `scripts/check.ps1` 增加静态回归检查。

## 4. 已验证项目

截至 `6e01a04`，本地已验证：

- `git diff --check`: 通过
- `scripts/check.ps1` Windows PowerShell 5.1: 通过
- `scripts/check.ps1` pwsh 7.6.1: 通过
- PowerShell 全量 parse: 通过
- API 无 `Response` 异常回归: 通过
- `Start-Here.ps1 -NonInteractive -SkipDisclaimer -TestSafe`: 通过
- `doctor.ps1` 沙盒假 `claude doctor` 睡 35 秒: 约 30 秒自动跳过，诊断继续

历史全量验收在 `4433b64` 时通过，包含：

- Start-Here TestSafe
- repair-deps TestSafe
- Start-Here -FixDeps TestSafe
- doctor ShareSafe
- 路径风险矩阵
- RemoveDeepSeekEnv + RestoreLatest 沙盒链路
- 真实 settings.json 保护
- release ZIP 构建
- ZIP forbidden/sensitive/real path 扫描
- ZIP 解压后 TestSafe/ShareSafe 运行
- ZIP 内 `install_wsl.sh` LF 规范化和 bash 检查

## 5. 发布前仍需执行

由于 `4433b64` 之后又有 `369a9cf`、`e708863`、`6e01a04`，最终发布前必须重新执行完整 Windows 原生自动化全量验收。

建议从以下命令开始：

```powershell
git fetch origin --prune
git checkout release/v1.3.2-rc
git pull --ff-only origin release/v1.3.2-rc
git status --short
git log --oneline --decorate -8
```

重点复查：

- 当前 HEAD 是 `6e01a04` 或更新。
- 真实 `%USERPROFILE%\.claude\settings.json` 前后 hash/length 不变。
- `git diff --check`。
- `scripts/check.ps1` 在 Windows PowerShell 5.1 和 pwsh 7.6.1 均通过。
- PowerShell 全量 parse。
- Start-Here TestSafe。
- repair-deps TestSafe。
- Start-Here -FixDeps TestSafe。
- doctor ShareSafe + SkipApiTest。
- 路径风险矩阵。
- RemoveDeepSeekEnv + RestoreLatest 沙盒链路。
- release ZIP 构建。
- ZIP 内容 forbidden/sensitive/real path 扫描。
- ZIP 解压后 `scripts/check.ps1`、Start-Here TestSafe、repair-deps TestSafe、doctor ShareSafe。

## 6. 注意事项

- 当前机器已安装 Claude Code 时，TestSafe 里看到 `Claude Code 已安装: 2.1.170 (Claude Code)` 是正常现象，不代表执行了真实安装。
- 不要使用真实 DeepSeek Key 做自动化验收。
- `doctor.ps1 -ShareSafe -SkipApiTest -NoOpenReport` 是可分享诊断验收路径。
- 如果源工作区 `bash -n install_wsl.sh` 因 Windows/WSL CRLF 环境限制失败，可记录为环境限制，不作为 Windows 自动化验收阻塞；但 release ZIP 内的 `install_wsl.sh` 必须为 LF 且 ZIP 内 bash 检查必须通过。
- 打包产物、报告、日志、备份、沙盒目录都不能提交。

## 7. 最后一次已知 ZIP 信息

在 `4433b64` 的全量验收中生成过：

- 文件: `release\ClaudeCode-DeepSeek-本地配置助手-v1.3.2.zip`
- SHA256: `98D7B6DE5CCFA5E35E932209EEB719FDB37FBCFCC4F5B8AC392FF9200ACC06C7`

该 ZIP 不应作为最终发布包继续使用，因为后续已有 `369a9cf`、`e708863`、`6e01a04` 修复。最终发布 ZIP 需要重新构建并记录新的 SHA256。
